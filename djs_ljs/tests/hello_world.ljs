import * as ffi from "ljs:ffi"

extern function puts(message: *ffi.c_char): ffi.c_int

export function main(): ffi.c_int {
  const msg: *ffi.c_char = ffi.c_str`Hello, world!`
  puts(msg)
  return 0
}

