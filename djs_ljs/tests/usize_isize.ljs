import * as c from "../libc.ljs";

let size_val: usize = 42;
let signed_size_val: isize = 0;

export function main(): void {
    c.printf(c.str`usize value: %zu\n`, size_val);
    c.printf(c.str`isize value: %zd\n`, signed_size_val);
}