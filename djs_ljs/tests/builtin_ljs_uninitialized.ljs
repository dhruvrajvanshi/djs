import * as c from "../libc.ljs"
import * as ljs from "ljs:builtin"

function main(): c.void {
    let message: *c.char = ljs.uninitialized
    message = c.str`PASS`
    c.puts(message)
    return 0
}
