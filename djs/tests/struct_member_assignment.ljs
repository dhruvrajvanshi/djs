import * as c from "ljs:libc"

struct StrBox {
    value: *c.char
}
function main(): c.int {
    const box = StrBox {
        value: c.str`FAIL`
    }
    box.value = c.str`PASS`
    c.puts(box.value)
    return 0
}
